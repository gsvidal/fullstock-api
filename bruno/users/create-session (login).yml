info:
  name: create-session (login)
  type: http

http:
  method: POST
  url: "{{base_url}}/api/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​