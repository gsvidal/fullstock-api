info:
  name: update-cart-item
  type: http

http:
  method: PATCH
  url: "{{base_url}}/api/cart/items/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​