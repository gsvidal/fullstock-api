info:
  name: get-categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
