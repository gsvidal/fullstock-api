info:
  name: get-product
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/products/:slug"
  params:
    - name: slug
      value: taza-git
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​